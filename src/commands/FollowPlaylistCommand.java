package commands;

public class FollowPlaylistCommand extends Command{
    public void execute() {
        if (this instanceof FollowPlaylistCommand) {
            FollowPlaylistCommand followPlaylistCommand = (FollowPlaylistCommand) this;
        }
    }
}
