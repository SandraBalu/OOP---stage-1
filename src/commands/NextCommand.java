package commands;

public class NextCommand extends Command{
    public void execute() {
        if (this instanceof NextCommand) {
            NextCommand nextCommand = (NextCommand) this;
        }
    }
}
